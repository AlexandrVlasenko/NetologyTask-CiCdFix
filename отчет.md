# Отчёт

**Какие изменения внесены.**  
Поправил пути и скрипты в workflow. Lockfile, lint, тесты из корня, dist/index.js, артефакт artifact.

**Как проверяли локально и в CI.**  
Локально: npm ci && npm run verify — ок. В CI не гонял: пуш в GitHub уже после всех правок.

**Какие ограничения текущего pipeline остались.**  
Деплой только с main. 
Staging может ждать approve. 
Node локально 24, в CI 20.

**Почему упал первый запуск CI.**  
Неверный package-locks.json.
 Потом ещё lint:ci и dist/main.js.

**Исправлял первопричины, а не симптомы.**  
Да. Не отключал шаги, чинил имена и пути.

**После каждой правки повторный запуск.**  
Локально да. CI — нет, репу не запушил.

**Порядок шагов логичен.**  
Да. checkout → node → ci → lint → test → build → smoke → artifact.

**Артефакты между job.**  
Да. web-dist → качается в artifact → deploy.sh.
